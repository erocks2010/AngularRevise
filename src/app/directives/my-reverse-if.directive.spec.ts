import { MyReverseIfDirective } from './my-reverse-if.directive';

describe('MyReverseIfDirective', () => {
  it('should create an instance', () => {
    const directive = new MyReverseIfDirective();
    expect(directive).toBeTruthy();
  });
});
