import { MyAtrributeDirectiveDirective } from './my-atrribute-directive.directive';

describe('MyAtrributeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAtrributeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
